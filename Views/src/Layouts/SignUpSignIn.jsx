import React from 'react';
import { Outlet } from 'react-router-dom';

const SignUpSignIn = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default SignUpSignIn;